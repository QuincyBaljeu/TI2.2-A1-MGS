<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ti22_a1_mgs">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Boundaries.MapActivity" />
        <activity android:name=".Boundaries.SettingsActivity" />
        <activity android:name=".Boundaries.PoiActivity" />
        <activity android:name=".Boundaries.RouteActivity" />
        <activity android:name=".Boundaries.HelpActivity" />
        <activity android:name=".Boundaries.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>